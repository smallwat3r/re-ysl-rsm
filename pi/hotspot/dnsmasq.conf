# DHCP for the hotspot clients, and every name resolves to the Pi so a phone's
# connectivity probe reaches pi/portal.py on port 80 and is redirected to the
# page (captive portal).
interface=wlan0
bind-dynamic
except-interface=lo
dhcp-range=10.42.0.10,10.42.0.250,12h
address=/#/10.42.0.1
