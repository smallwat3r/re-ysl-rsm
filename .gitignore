# Python
__pycache__/
.pytest_cache/
*.py[cod]
.venv/

# Vendored reverse-engineering deps (androguard, capstone, pyghidra; installed into .pylibs, see README)
.pylibs/

# Downloaded toolchain (Ghidra + JDK, ~1.3 GB, fetched separately)
tools/

# Proprietary app binaries and everything derived from them - do NOT commit
# (L'Oreal's APK and its extracted/decompiled contents). Regenerate with phone.sh + research/.
apk/
native/
decompiled/

# Personal captures and device dumps (contain device identifiers / personal data)
btsnoop_hci.log
bugreport.zip
btmon.txt

# Local runtime config: the device's BLE address and any LAN hostnames
.env

# Saved favourites (per-user data)
favourites.db

# Self-signed TLS key+cert for the hotspot portal (generated by pi/setup.sh)
pi/portal.pem
